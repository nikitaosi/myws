---
const { href, platform, isEmail } = Astro.props;
---

{isEmail ?
<a href={href} target="_blank" class='mt-2'>{platform}</a>
  :
<a href={href} target="_blank"
   class="after:content-['→'] after:text-2xl after:ml-1 after:opacity-0 hover:after:opacity-100 after:transition-all">{platform}</a>}

